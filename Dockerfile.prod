# Production Dockerfile - Uses pre-built Vite files
# This is used by GitHub Actions CI/CD
# Build is done on GitHub Actions, not in Docker

FROM nginx:alpine

# Nginx 설정
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 미리 빌드된 파일 복사 (GitHub Actions에서 빌드됨)
COPY dist /usr/share/nginx/html

# 권한 설정
RUN chmod -R 755 /usr/share/nginx/html

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
